// Stub for react-native-android-widget — satisfied by Metro when the real
// package is not installed (Expo Go / iOS builds).
const React = require('react');

const FlexWidget  = () => null;
const TextWidget  = () => null;
const ImageWidget = () => null;

module.exports = { FlexWidget, TextWidget, ImageWidget };
